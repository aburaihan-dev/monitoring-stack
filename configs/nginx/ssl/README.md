Place your existing SSL certificate here before running `./stack proxy ssl`:

- `fullchain.pem` — certificate (+ chain)
- `privkey.pem`   — private key

Both files are gitignored; only this README is tracked.
